# MLB 預測模型公式說明

更新時間：2026-07-10  
專案：MLB 實戰投注計畫  
用途：整理目前系統內實際使用的勝方、比分、大小分、蒙地卡羅、進階因子與賽後校正公式。

> 注意：這是目前程式內的實際邏輯整理，不代表保證獲利。模型結果應搭配台灣運彩真實盤口、賽前先發打線、天氣、牛棚狀態與賽後檢討持續修正。

## 1. 主要勝方模型

每日勝方預測主模型目前使用：

- 主模型：A-畢氏勝率
- 確認模型：E-對照組 Ensemble
- 主推門檻：由近十天賽後檢討動態調整，目前為 57%

### A-畢氏勝率模型

先計算每隊的畢氏勝率：

```text
畢氏勝率 = 得分^1.83 / (得分^1.83 + 失分^1.83)
```

主隊勝率：

```text
主隊勝率 = 主隊畢氏勝率 / (主隊畢氏勝率 + 客隊畢氏勝率)
```

加入主場修正：

```text
模型勝率 = 主隊勝率 + 0.02
```

最後限制範圍：

```text
模型勝率 = min(0.75, max(0.35, 模型勝率))
```

如果主隊勝率 >= 50%，預測主隊；否則預測客隊。

## 2. 其他勝方輔助模型

### B-近期狀態

使用最近 10 場勝率：

```text
近期勝率 = 最近10場勝場 / 最近10場場數
主隊勝率 = 主隊近期勝率 / (主隊近期勝率 + 客隊近期勝率)
模型勝率 = 主隊勝率 + 0.015
```

限制範圍：

```text
模型勝率 = min(0.75, max(0.35, 模型勝率))
```

### C-ELO 評分

ELO 預期主隊勝率：

```text
主隊勝率 = 1 / (1 + 10 ^ ((客隊ELO - 主隊ELO) / 400))
模型勝率 = 主隊勝率 + 0.015
```

ELO 每場更新：

```text
主隊ELO += 20 * (主隊實際結果 - 主隊預期勝率)
客隊ELO += 20 * (客隊實際結果 - 客隊預期勝率)
```

### D-近期失分代理

使用最近 5 場平均失分：

```text
主隊勝率 = 客隊近5場平均失分 / (主隊近5場平均失分 + 客隊近5場平均失分)
模型勝率 = 主隊勝率 + 0.02
```

限制範圍：

```text
模型勝率 = min(0.70, max(0.38, 模型勝率))
```

### E-對照組 Ensemble

把 A/B/C/D 的預測依最近 30 次準確率加權：

```text
模型權重 = max(0.1, 最近30次準確率^2)
Ensemble勝率 = sum(各模型勝率 * 權重) / sum(權重)
```

限制範圍：

```text
Ensemble勝率 = min(0.75, max(0.35, Ensemble勝率))
```

## 3. 勝方推薦規則

每日預測不只看 A 模型，還會檢查：

```text
A模型預測方向 == Ensemble確認方向
信心 >= 近十天校正後主推門檻
比分模型方向 == 勝方模型方向
```

目前近十天校正後：

```text
主推勝方最低信心 = 57%
```

若方向分歧，會降級為「模型分歧」或「不推薦」。

## 4. 先發投手資料權重

先發投手來源分三種：

```text
官方公布先發：權重 0.90
輪值推估先發：權重約 0.35 ~ 0.75
未公布且無法推估：權重 0.00，回到聯盟平均
```

投手 profile 會向聯盟平均收斂：

```text
修正後投手數值 = 聯盟平均 + (投手原始數值 - 聯盟平均) * 投手權重
```

也就是官方先發影響最大，輪值推估次之，完全未知則不把投手當成可靠優勢。

## 5. 進階因子勝方模型

進階因子模型會計算主客隊分數，再轉成勝率。

### 打擊率壘包因子

```text
總壘包 = H + 2B + 2*3B + 3*HR
TB/AB = 總壘包 / 打數
K率 = 三振 / 打席
BB率 = 四壞 / 打席
GIDP率 = 雙殺打 / 打席
```

打擊分數：

```text
打擊率壘包 =
4.0 * (AVG - 0.245)
+ 2.5 * (OBP - 0.315)
+ 2.0 * (SLG - 0.400)
+ 1.2 * (TB/AB - 0.40)
+ 1.8 * BB率
- 1.6 * K率
- 5.0 * GIDP率
```

### 先發投手因子

```text
K9 = 三振 * 9 / 投球局數
BB9 = 四壞 * 9 / 投球局數
HR9 = 全壘打 * 9 / 投球局數
滾地率 = 滾地出局 / (滾地出局 + 飛球出局)
```

投手分數：

```text
投手分數 =
0.22 * (4.3 - ERA)
+ 0.45 * (1.30 - WHIP)
+ 0.035 * (K9 - 8.3)
- 0.05 * (BB9 - 3.2)
- 0.08 * (HR9 - 1.1)
+ 0.25 * (滾地率 - 0.48)
```

實際使用時再乘上先發投手權重。

### 牛棚深度代理

目前先用全隊投球資料與近期失分代理牛棚，尚未做到逐名牛棚可用性：

```text
牛棚分數 =
0.16 * (4.2 - ERA)
+ 0.30 * (1.32 - WHIP)
+ 0.025 * (K9 - 8.5)
- 0.035 * (BB9 - 3.4)
+ 0.06 * 近期對手失分脈絡
```

### 近期戰績

```text
近期得失分差 = 最近N場平均(得分 - 失分)
近期戰績分數 = 0.055 * 近期得失分差
```

### 連勝連敗

最近 8 場連勝連敗：

```text
若連勝：連勝分數 = 連勝場數 / 8
若連敗：連敗分數 = -連敗場數 / 8
模型使用 = 0.28 * 連勝連敗分數
```

### 對戰組合

最近 10 場對戰：

```text
對戰分數 = 該隊對戰勝率 - 0.5
模型使用 = 0.18 * 對戰分數
```

### 場地天氣代理

目前還沒有真實天氣，先用主場歷史總分代理：

```text
球場總分差 = 主場最近40場平均總分 - 聯盟主場平均總分
場地因子 = 球場總分差 / 4
模型使用 = 0.02 * 場地因子
```

### 主場因子

```text
主隊額外加分 = 0.09
```

### 進階模型勝率

```text
客隊進階分數 = 客隊各因子加總
主隊進階分數 = 主隊各因子加總
主隊勝率 = sigmoid(主隊進階分數 - 客隊進階分數)
```

其中：

```text
sigmoid(x) = 1 / (1 + e^(-x))
```

限制範圍：

```text
主隊勝率 = min(0.70, max(0.30, 主隊勝率))
```

## 6. 打者與投手 profile

### 打者 profile

```text
contact = 1 + (AVG - 0.245) * 2.4 - K率 * 0.55
power = 1 + (SLG - 0.400) * 1.7 + (TB/AB - 0.40) * 0.55
patience = 1 + (OBP - 0.315) * 2.2 + BB率
gidp = 1 + GIDP率 * 8.0
```

限制範圍：

```text
contact: 0.72 ~ 1.35
power: 0.70 ~ 1.45
patience: 0.72 ~ 1.35
gidp: 0.75 ~ 1.35
```

### 投手 profile

```text
k_factor = 1 + (K9 - 8.3) / 16
bb_factor = 1 + (BB9 - 3.2) / 14
hr_factor = 1 + (HR9 - 1.1) / 5
gb_factor = 1 + (滾地率 - 0.45) * 0.9
run_prevention_factor = 1 + (ERA - 4.3) / 10 + (WHIP - 1.30) / 4
```

限制範圍：

```text
k_factor: 0.80 ~ 1.25
bb_factor: 0.82 ~ 1.22
hr_factor: 0.78 ~ 1.28
gb_factor: 0.85 ~ 1.20
run_prevention_factor: 0.75 ~ 1.30
```

## 7. 蒙地卡羅逐打席模擬

每場比賽模擬 10,000 次。每一個打席依加權機率抽結果。

基礎事件權重：

```text
一壘安打 15.2
二壘安打 4.7
三壘安打 0.5
全壘打 3.3
四壞 8.2
三振 22.0
滾地出局 20.0
飛球出局 17.0
平飛出局 7.0
```

打席事件權重：

```text
一壘安打 = 15.2 * offense * batter.contact
二壘安打 = 4.7 * offense * batter.power
三壘安打 = 0.5 * offense * batter.power
全壘打 = 3.3 * offense * batter.power * team.power * pitcher.hr_factor
四壞 = 8.2 * batter.patience * pitcher.bb_factor
三振 = 22.0 * (1.02 / batter.contact) * prevention * pitcher.k_factor
滾地出局 = 20.0 * prevention * pitcher.gb_factor
飛球出局 = 17.0
平飛出局 = 7.0
```

如果一壘有人且少於兩出局，加入雙殺打：

```text
雙殺打 = 5.8 * batter.gidp * prevention * pitcher.gb_factor
```

其中：

```text
offense = 球隊攻擊分 / 4.45
prevention = (對手防守/失分分 / 4.45) * 投手run_prevention_factor
```

每次模擬跑完整場，最後統計：

```text
主隊勝率 = 主隊贏的次數 / 模擬次數
客隊勝率 = 客隊贏的次數 / 模擬次數
平均主隊得分 = 主隊總得分 / 模擬次數
平均客隊得分 = 客隊總得分 / 模擬次數
平均總分 = 平均主隊得分 + 平均客隊得分
```

## 8. 大小分判斷

大小分會參考：

- 台灣運彩大小分盤口
- 大小分模型預測總分
- 蒙地卡羅平均總分
- 勝方比分模型總分
- 近十天賽後校正

基本方向：

```text
若預測總分 > 盤口：大分
若預測總分 < 盤口：小分
若預測總分 = 盤口：平盤
```

目前近十天校正後，推薦大小分必須符合：

```text
大小分模型方向 == 蒙地卡羅/比分方向
大小分機率 >= 59%
edge >= 3%
修正後預測總分距離盤口 >= 1.25 分
```

若不符合，顯示「不推薦大小分」。

## 9. 賽後檢討校正

系統會讀最近 10 天賽後結果，統計錯誤原因。

目前 reason codes 包含：

```text
high_confidence_hit       高信心命中
high_confidence_miss      高信心失準
low_confidence_hit        低信心命中
low_confidence_miss       低信心失準
one_run_volatility        一分差高波動
totals_miss               大小分錯誤
large_totals_error        大小分大偏差
score_totals_model_gap    勝方比分模型與大小分模型分歧
roi_loss                  投注單虧損
roi_win                   投注單獲利
pending                   尚未結算
```

### 勝方門檻校正

```text
若近十天低信心勝方準確率 < 52%：
    勝方主推門檻提高到 56%

若近十天高信心失準 >= 5 場：
    勝方主推門檻提高到至少 57%
```

### 比分總分偏差校正

```text
總分偏差 = 平均(實際總分 - 預測總分)
套用校正 = clamp(總分偏差, -0.75, +1.25)
```

目前最近 10 天：

```text
總分偏差約 +0.66
修正後預測總分 = 原始預測總分 + 0.66
```

### 大小分門檻校正

若最近 10 天「勝方比分模型與大小分模型分歧」太多：

```text
大小分最低機率 = 59%
大小分最低 edge = 3%
大小分離盤門檻 = 1.25 分
```

## 10. 目前模型缺口

根據最近 10 天檢討，最大問題不是單純權重，而是資料缺口：

```text
score_totals_model_gap 很多：
    勝方比分模型與大小分模型不是同一套得分期望。

totals_miss / large_totals_error 很多：
    缺真實天氣、風向、球場環境、牛棚臨場狀態。

high_confidence_miss 偏多：
    缺官方先發打線、牛棚可用性、傷兵/輪休。
```

下一步建議：

```text
1. 建立統一得分期望模型，讓勝方、比分、大小分共用同一套得分來源。
2. 補真實天氣、球場風向、溫度、濕度。
3. 補官方先發打線與球員缺陣資訊。
4. 補牛棚近 3 天用量、可用投手、連投狀態。
5. 賽後檢討用 reason_codes 回寫各因子的校正，而不是只做總分 +0.66。
```

